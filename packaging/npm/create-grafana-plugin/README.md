# create-grafana-plugin

Scaffold production-ready Grafana plugin projects (Rspack, Bun, optional Rust WASM).

Full documentation: <https://github.com/andeya/create-grafana-plugin>

This package ships a small Node entry script; the native CLI is installed via platform-specific optional dependencies.
