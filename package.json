{
  "name": "create-grafana-plugin",
  "version": "0.2.6",
  "description": "CLI tool to scaffold production-ready Grafana plugin projects with Rspack, bun, optional Rust WASM, Docker dev environment, and mock data generator.",
  "author": "andeya <andeyalee@gmail.com>",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/andeya/create-grafana-plugin"
  },
  "keywords": [
    "grafana",
    "plugin",
    "scaffold",
    "create",
    "rspack",
    "wasm",
    "rust",
    "cli"
  ],
  "packageManager": "bun@1.3.11",
  "bin": {
    "create-grafana-plugin": "packaging/npm/create-grafana-plugin/bin/index.js"
  },
  "scripts": {
    "format": "bun run format:ts && bun run format:rust",
    "format:ts": "biome format --write scripts/ packaging/",
    "format:rust": "cargo fmt --all",
    "format:check": "bun run format:check:ts && bun run format:check:rust",
    "format:check:ts": "biome format scripts/ packaging/",
    "format:check:rust": "cargo fmt --all -- --check",
    "lint": "bun run lint:ts && bun run lint:rust",
    "lint:ts": "biome check scripts/ packaging/",
    "lint:rust": "cargo clippy --workspace --all-targets -- -D warnings",
    "lint:fix": "bun run lint:fix:ts && bun run lint:fix:rust",
    "lint:fix:ts": "biome check --write --unsafe scripts/ packaging/",
    "lint:fix:rust": "cargo fmt --all && cargo clippy --workspace --all-targets --fix --allow-dirty -- -D warnings",
    "test": "cargo test --workspace",
    "verify": "bun run lint && bun run test",
    "bump": "bun scripts/bump-version.ts",
    "bump:patch": "bun scripts/bump-version.ts patch",
    "bump:minor": "bun scripts/bump-version.ts minor",
    "bump:major": "bun scripts/bump-version.ts major"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.9",
    "bun-types": "^1.3.11"
  }
}